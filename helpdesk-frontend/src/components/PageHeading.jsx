function PageHeading({ text }){
    return (
        <div>
            {/* Displays whatever text is passed in as a prop */}
            <h1>{text}</h1>
        </div>
    );
}
export default PageHeading;